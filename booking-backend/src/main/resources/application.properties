# Datenbank-Verbindung (URL)
spring.datasource.url=jdbc:mysql://localhost:3306/booking_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Benutzername und Passwort (DEIN Passwort hier eintragen!)
spring.datasource.username=root
spring.datasource.password=Goodhiba1+

# WICHTIG für deinen Fehler: Wir sagen Spring explizit, welchen Treiber es nutzen soll
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Einstellungen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect